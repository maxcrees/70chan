* Move default deletion password out of schema.sql and into config.ini
* Per-board settings in config.ini
* Make POSTING file dynamic

Far future:
* Admin from browser??
* Support query strings as well as default slashes
* "User was banned for this post"
